{
	"mac": {
		"icon": "build/icon.icns",
		"publish": [
			{
				"provider": "github",
				"releaseType": "prerelease"
			}
		]
	},
	"win": {
		"icon": "build/icon.ico",
		"publish": [
			{
				"provider": "github",
				"releaseType": "prerelease"
			}
		]
	},
	"directories": {
		"output": "../dist/${platform}"
	},
	"artifactName": "${productName}.${ext}"
}
